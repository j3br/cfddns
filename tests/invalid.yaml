auth:
  api_token: ""  # Empty token is invalid
  api_key:
    key: my_api_key
    email: my_email@example.com

zone_id: my_zone_id

subdomains:
  example.com:
    proxied: true
    ttl: 1.1  # Invalid TTL value

